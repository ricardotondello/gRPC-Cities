version: "3.7"
services:

  grpcserver:
    container_name: grpcserver
    build:
      context: .
      dockerfile: grpcserver/Dockerfile
    networks:
      grpc_network:
    ports:
      - "5000:80"
      
networks:
  grpc_network: